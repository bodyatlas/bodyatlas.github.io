{
  "name": "bodyatlas",
  "private": true,
  "description": "Tooling only. The site itself is static and needs no build to view.",
  "scripts": {
    "build:parts": "node tools/build-part-pages.js"
  },
  "devDependencies": {
    "three": "0.160.0"
  }
}
